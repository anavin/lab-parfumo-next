node_modules
.next

# env files (local secrets) — block ทุก variant ยกเว้น .example
.env
.env.local
.env.development.local
.env.production.local
.env.test.local
!.env.local.example
!.env.example

.DS_Store

# typescript build artifact
next-env.d.ts
.vercel
tsconfig.tsbuildinfo
backups/

# Cloudflare Workers / OpenNext build artifacts + local wrangler state
.open-next
.wrangler
.dev.vars
.dev.vars.*
# extra env variants (main list above blocks .env / .env.local — also block prod inline)
.env.production
.env.staging
